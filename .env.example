# Porta em que o Uvicorn escuta (a maioria das plataformas de deploy injeta isso automaticamente)
PORT=8000

# Endereço em que o Uvicorn escuta
HOST=0.0.0.0

# Caminho do arquivo SQLite. Se não definido, usa "gastos.db" na raiz do projeto.
DATABASE_PATH=gastos.db
